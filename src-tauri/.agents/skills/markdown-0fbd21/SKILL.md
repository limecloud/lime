---
name: 我来读取已导出的 Markdown 文件，然后翻...
description: 沉淀自一次成功结果：我来读取已导出的 Markdown 文件，然后翻译成中文。 文件已读取完毕。现在我将正文翻译为中文，保留所有 Markdown 结构、代码块、链...
---

# 我来读取已导出的 Markdown 文件，然后翻...

## 何时使用
- 当你需要继续产出“我来读取已导出的 Markdown 文件，然后翻...”这类结果时使用。
- 适合继续沿用这次围绕“我来读取已导出的 Markdown 文件，然后翻译成中文”的结构、判断方式和交付颗粒度。

## 输入
- 目标与主题：我来读取已导出的 Markdown 文件，然后翻译成中文
- 补充受众、风格、篇幅、平台或输出格式等关键约束。
- 如有历史版本、参考资料、示例或素材，可一并提供。

## 执行步骤
1. 先确认本次任务是否仍围绕：我来读取已导出的 Markdown 文件，然后翻译成中文
2. 提炼这次成功结果里的结构骨架，再决定哪些部分需要沿用或改写。
3. 按相同颗粒度补齐关键信息与执行细节，输出可直接交付的结果。

## 输出
- 交付一份与“我来读取已导出的 Markdown 文件，然后翻...”同类型、可直接使用的完整结果。
- 输出需要覆盖“我来读取已导出的 Markdown 文件，然后翻译成中文”对应的关键信息，并保持结构清晰。
- 必要时附带简短说明，便于继续复用或二次迭代。

## 失败回退
- 如果用户目标或素材不足，先补问最关键的主题、受众或平台约束。
- 如果原结果不适合直接复用，先提炼最小骨架，再给出可继续迭代的首版。
- 如果信息仍然缺失，明确标注待补内容，不要假设不存在的事实。

## 维护提示
- 如需引用资料，请将文件放到 `references/` 目录。
- 如需脚本或素材，请分别放到 `scripts/` 与 `assets/` 目录。
- 如需长期沉淀模板或示例，优先放到相邻目录，不要把所有细节都塞进主文件。
