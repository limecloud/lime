# Lime CLI 概览

Lime CLI 的目标是把任务型业务收敛为统一的命令边界，优先服务 Agent、技能和独立测试。

适合 CLI 化的能力：

- 输入输出稳定
- 结果可结构化 JSON
- 可异步提交并通过状态查询跟踪
- 适合 `skills + bash` 透明执行

当前主线业务：

- `image`
- `cover`
- `video`
- `broadcast`
- `url-parse`
- `typesetting`
- `resource-search`
